int counter = 50;
int NUM_DISPLAY = 1;
void HAL_TIM_PeriodElapsedCallback(TIM_HandleTypeDef *htim)
{
	counter--;
	if (counter <= 0)
	{
		counter = 50;
		display7SEG(NUM_DISPLAY);
		if (NUM_DISPLAY == 2)
		{
		  HAL_GPIO_WritePin(EN0_GPIO_Port, EN0_Pin, SET);
		  HAL_GPIO_WritePin(EN1_GPIO_Port, EN1_Pin, RESET);
		}

		if (NUM_DISPLAY == 1)
		{
		  HAL_GPIO_WritePin(EN0_GPIO_Port, EN0_Pin, RESET);
		  HAL_GPIO_WritePin(EN1_GPIO_Port, EN1_Pin, SET);
		  HAL_GPIO_TogglePin(LED_RED_GPIO_Port, LED_RED_Pin);
		}

		NUM_DISPLAY++;
		if (NUM_DISPLAY > 2) NUM_DISPLAY = 1;
	}
}




The frequency of the scan is 1 Hz. Since there are two LEDs and each LED is on for 0.5 seconds, the total scan period is 1 second. So the frequency is: freq = 1 / T = 1 / 1 = 1 (Hz)