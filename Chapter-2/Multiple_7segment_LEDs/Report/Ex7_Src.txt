  setTimer(0, 10);
  setTimer(1, 50);
  while (1)
  {
	  if (timer_flag[0] == 1)
	  {
		  HAL_GPIO_TogglePin(LED_RED_GPIO_Port, LED_RED_Pin);
		  setTimer(0, 2000);
	  }

	  if (timer_flag[1] == 1)
	  {
		  HAL_GPIO_TogglePin(DOT_GPIO_Port, DOT_Pin);
		  second++;
		  if (second >= 60)
		  {
			  second = 0;
			  minute++;
		  }
		  if(minute >= 60)
		  {
			  minute = 0;
			  hour++;
		  }
		  if(hour >=24)
		  {
			  hour = 0;
		  }
		  updateClockBuffer();
		  setTimer(1, 1000);
	  }
}